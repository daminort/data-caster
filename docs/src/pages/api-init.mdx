---
name: init
route: /api-init
menu: API reference
---

import { Code } from '../components/Code';
import { Comparison } from '../components/Comparison';
import { signature, options, example, before, after } from '../code/api-init';

# init

Makes initialization of `DataCaster` instance.

> This method is chainable.

## Signature

<Code>
    {signature}
</Code>

`options` is an object that can be omitted and thus it will use options by default:

<Code>
    {options}
</Code>

If you don't need to replace default options you can omit using this method at all.

## Example

<Code>
    {example}
</Code>

<Comparison
    before={before}
    after={after}
/>
